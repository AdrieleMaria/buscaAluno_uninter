#include <stdio.h>
#include <locale.h>

#include "bst.h"

int main(void) {

  // utilização da biblioteca locale
  setlocale(LC_ALL, "");    

  arvore *raiz; // raiz é um ponteiro para ponteiro para a struct no

  printf("Hello World\n");
  return 0;
}